# 复制为仓库外或本机忽略的环境配置，并填写至少 32 个字符的随机值。
NUXT_SESSION_PASSWORD=

# 开发环境使用 development 以允许 HTTP 远程联调；生产环境必须设为 production 并使用 HTTPS。
NODE_ENV=development

# 可选：覆盖默认的 ./data 运行时数据目录。
NUXT_DATA_DIRECTORY=./data

# HTTP 服务默认仅监听本机 3001 端口，避免与常见开发服务的 3000 端口冲突。
# 可按需修改 HOST 与 PORT；生产环境应继续仅监听回环地址。
HOST=127.0.0.1
PORT=3001

# 反向代理不能保留原始 Host 时，填写浏览器访问的 HTTPS 域名；多个域名用英文逗号分隔。
# 留空表示仅接受后端根据请求头识别出的同源请求。
NUXT_TRUSTED_BROWSER_ORIGINS=

# 默认关闭低风险人物修订自动发布；开启后仍必须先通过全部固定评测门禁。
NUXT_FEEDBACK_AUTO_PUBLISH_LOW_RISK=false

# 全局 HTTP 请求正文和文件写入后的最小磁盘余量；单位均为字节。
NUXT_LIMITS_REQUEST_BODY_BYTES=2200000
NUXT_LIMITS_MINIMUM_FREE_DISK_BYTES=104857600

# 本地 JSON Lines 日志按大小轮转，并清理超过保留天数的轮转文件。
NUXT_LOGGING_MAXIMUM_FILE_BYTES=5242880
NUXT_LOGGING_RETENTION_DAYS=14
