# 接口与任务状态机

## 接口原则

- 路径统一使用 `/api/v1` 前缀。
- 控制器只处理会话校验、Zod 请求解析、应用服务调用和 HTTP 映射。
- 同步 CRUD 成功返回 `200` 或 `201`；异步运行创建返回 `202` 和任务标识。
- 删除成功返回 `204`，删除前的影响分析使用独立只读接口。
- 时间使用 UTC Unix 毫秒，标识使用字符串 UUID。
- 外部接口只暴露业务对象，不暴露 Drizzle 行、模型 SDK 响应或 OpenViking 原始响应。

## 响应格式

成功响应：

```json
{
  "data": {}
}
```

失败响应：

```json
{
  "error": {
    "code": "PERSONA_VERSION_NOT_ACTIVE",
    "message": "所选人物版本不可用于新运行",
    "details": {}
  }
}
```

`message` 面向用户，`code` 供界面稳定判断；内部堆栈、模型密钥和原始外部响应不得返回浏览器。

## 认证接口

| 方法与路径 | 用途 | 应用服务 |
|---|---|---|
| `GET /api/v1/setup/status` | 判断是否需要首次设置 | `AuthApplicationService.getSetupStatus` |
| `POST /api/v1/setup/admin` | 创建唯一管理员 | `AuthApplicationService.setupAdministrator` |
| `POST /api/v1/auth/login` | 密码登录并建立会话 | `AuthApplicationService.login` |
| `POST /api/v1/auth/logout` | 清除当前会话 | `AuthApplicationService.logout` |
| `GET /api/v1/auth/session` | 获取当前登录状态 | `AuthApplicationService.getSession` |

首次设置接口只在管理员不存在时可用。除设置状态、首次设置和登录外，其余接口都要求有效会话。

## 世界、人物和资料接口

| 方法与路径 | 用途 |
|---|---|
| `GET/POST /api/v1/worlds` | 查询或创建世界设定 |
| `GET/PATCH /api/v1/worlds/{worldId}` | 查看或更新世界元数据 |
| `GET/POST /api/v1/worlds/{worldId}/versions` | 查询版本或创建候选版本 |
| `POST /api/v1/world-versions/{versionId}/publish` | 发布并切换当前世界版本 |
| `DELETE /api/v1/world-versions/{versionId}` | 删除非当前、无后续修改且无历史任务引用的世界版本 |
| `GET /api/v1/world-versions/compare` | 比较两个不可变世界版本 |
| `POST /api/v1/worlds/{worldId}/rollback` | 切换到指定历史已发布版本 |
| `GET /api/v1/worlds/{worldId}/deletion-impact` | 删除影响分析 |
| `DELETE /api/v1/worlds/{worldId}` | 删除无人物关联的世界 |
| `GET/POST /api/v1/personas` | 查询或创建人物 |
| `POST /api/v1/personas/draft` | 从自然语言、可选世界和参考资料生成不落库候选草稿 |
| `GET/PATCH /api/v1/personas/{personaId}` | 查看或更新人物元数据 |
| `GET /api/v1/personas/{personaId}/deletion-impact` | 删除影响分析 |
| `DELETE /api/v1/personas/{personaId}` | 永久删除人物及私有历史 |
| `GET/POST /api/v1/personas/{personaId}/versions` | 查询版本或创建候选版本 |
| `POST /api/v1/persona-versions/{versionId}/publish` | 发布并切换当前版本 |
| `GET /api/v1/persona-versions/compare` | 比较两个不可变人物版本 |
| `POST /api/v1/personas/{personaId}/rollback` | 切换到指定历史已发布版本 |
| `GET/POST /api/v1/sources` | 查询或创建粘贴文本资料 |
| `POST /api/v1/sources/files` | 导入 TXT 或 Markdown 文件 |
| `GET/PATCH /api/v1/sources/{sourceId}` | 查看或更新资料元数据与正文 |
| `GET /api/v1/sources/{sourceId}/deletion-impact` | 删除影响分析 |
| `GET /api/v1/sources/search` | 使用 SQLite FTS5 检索资料切片 |
| `POST /api/v1/sources/{sourceId}/links` | 关联人物或世界 |
| `DELETE /api/v1/sources/{sourceId}/links/{linkId}` | 解除关联 |
| `DELETE /api/v1/sources/{sourceId}` | 删除无关联资料 |

自然语言整理与持久化创建是两个明确动作：`POST /personas/draft` 只返回待编辑草稿，不创建人物；用户检查结构化字段后再调用 `POST /personas` 保存初始候选版本，最后另行发布。资料型草稿至少选择一项资料，一次最多向模型提供 8 项资料。

## 模板与参数接口

| 方法与路径 | 用途 |
|---|---|
| `GET/POST /api/v1/format-templates` | 查询或创建格式模板 |
| `GET/POST /api/v1/parameter-profiles` | 查询或创建参数方案 |

模板和参数方案只通过创建接口增加不可变版本，不提供原地修改接口。运行创建时选择一个完整方案，或使用系统默认参数。

## 兴趣与生成运行接口

| 方法与路径 | 用途 |
|---|---|
| `POST /api/v1/interest-runs` | 创建异步兴趣判断，返回 `202` |
| `POST /api/v1/generation-runs` | 创建图文运行并生成文档规格，返回 `202` |
| `GET /api/v1/runs` | 按人物、类型、状态和数量上限查询运行 |
| `GET /api/v1/runs/{runId}` | 获取运行快照、任务和结果 |
| `POST /api/v1/runs/{runId}/cancel` | 请求协作式取消 |
| `POST /api/v1/runs/{runId}/retry` | 对可重试失败创建新任务 |
| `PUT /api/v1/runs/{runId}/document-spec` | 修改待确认文档规格 |
| `POST /api/v1/runs/{runId}/document-spec/confirm` | 确认规格并创建执行任务 |
| `POST /api/v1/runs/{runId}/blocks/{blockId}/attempts` | 生成新的块尝试 |
| `POST /api/v1/runs/{runId}/blocks/{blockId}/select` | 选择某次成功尝试 |
| `POST /api/v1/runs/{runId}/blocks/{blockId}/lock` | 锁定或解除锁定块 |
| `POST /api/v1/runs/{runId}/render` | 渲染指定格式 |
| `GET /api/v1/runs/{runId}/assets/{assetId}` | 读取该运行引用的受控图片资产 |
| `GET /api/v1/runs/{runId}/exports/{format}` | 下载文件或资源包 |

前端通过 `GET /runs/{runId}` 轮询状态，活动运行默认每两秒一次；MVP 不实现 WebSocket 或 SSE。

## 反馈与评测接口

| 方法与路径 | 用途 |
|---|---|
| `POST /api/v1/runs/{runId}/feedback` | 记录反馈并返回 AI 分类建议 |
| `GET /api/v1/feedback` | 查询原始反馈、AI 建议和用户确认历史 |
| `POST /api/v1/feedback/{feedbackId}/classify` | 用户确认或纠正反馈目标 |
| `GET /api/v1/revision-proposals` | 查询修订提案 |
| `GET /api/v1/revision-proposals/{proposalId}` | 查看差异、证据和风险 |
| `POST /api/v1/revision-proposals/{proposalId}/evaluate` | 创建评测任务 |
| `POST /api/v1/revision-proposals/{proposalId}/publish` | 按门禁发布候选版本 |
| `POST /api/v1/revision-proposals/{proposalId}/reject` | 拒绝并记录原因 |
| `GET/POST /api/v1/personas/{personaId}/evaluation-cases` | 管理人物回归用例 |
| `GET /api/v1/personas/{personaId}/memories` | 查询人物候选、有效、停用和拒绝记忆 |
| `PATCH /api/v1/personas/{personaId}/memories` | 确认、拒绝、停用或恢复一条人物记忆 |
| `GET /api/v1/evaluation-runs/{evaluationRunId}` | 查看逐用例评测结果 |

高风险提案即使评测通过也不能自动调用发布接口。

## 系统与备份接口

| 方法与路径 | 用途 |
|---|---|
| `GET /api/v1/system/capabilities` | 文本、图片、OpenViking、当前检索方式和默认运行参数 |
| `GET /api/v1/system/health` | 查询 SQLite、Worker 和持久任务队列摘要 |
| `GET /api/v1/system/audit` | 查询关键管理与维护动作审计 |
| `POST /api/v1/system/providers/check` | 主动检测 OpenViking 健康状态，不返回密钥 |
| `GET /api/v1/system/context/status` | 查询 OpenViking 能力与逐资料范围投影同步状态 |
| `POST /api/v1/system/context/reindex` | 清理旧共享目录及已知精确投影，再从 SQLite 全量重建 |
| `POST /api/v1/system/backups` | 同步创建并验证一致性备份，返回 `201` 摘要 |

备份验证和实际恢复只通过本机 CLI 执行。恢复要求应用已经停止，不通过普通在线接口覆盖当前数据。

## 运行状态机

图文运行状态：

```text
planning
  ├──失败──> failed
  └──完成──> awaiting_confirmation
                   ├──取消──> canceled
                   └──确认──> queued
                                  ↓
                               running
                    ┌─────────────┼─────────────┐
                    v             v             v
                succeeded      partial        failed
                    └────取消请求安全点──────> canceled
```

兴趣运行不经过 `awaiting_confirmation`，创建后直接进入 `queued`。

任务状态：

```text
queued → running → succeeded
   │         ├────→ failed
   │         └────→ cancel_requested → canceled
   └──────────────→ canceled
```

`partial` 表示至少一个块成功且至少一个块失败；成功块和尝试必须保留。

## Worker 规则

1. 单进程内只启动一个 Worker 循环。
2. 领取任务时以短事务把 `queued` 更新为 `running`，增加尝试次数并写入租约。
3. 外部调用期间不保持数据库事务；任务依赖领取时写入的租约和重启恢复处理异常退出。
4. 生成和评测任务最多两次自动尝试；`sync_context_source` 与 `sync_openviking_session` 最多三次，耗尽后进入 `failed` 并保留同步错误。
5. 手工重试创建新的任务记录或新的块尝试，不重置旧历史。
6. 取消是协作式的：在模型调用前后、块之间和文件写入前检查取消标志。
7. 进程重启时，租约过期且仍可尝试的任务重新进入 `queued`；超过上限的任务进入 `failed` 并记录恢复原因。

## 限制与超时

每次运行解析并保存以下上限：

- 最大文字块数。
- 最大图片块数。
- 单次模型提示最大字符数。
- 单次模型最大输出 Token。
- 整次运行最大已报告 Token。
- 模型请求超时。
- 单块最大尝试数。

提示输入不使用本地 Token 估算：不同供应商和模型的分词器并不一致，调用前只按系统提示与用户提示的准确字符总数执行硬限制。整次运行 Token 上限仅累计供应商实际报告的值；供应商未返回的字段显示为未知，不伪造估算值。

单块尝试数是该块跨自动执行、整文重试和手工单块重试的累计上限，不能通过新建任务绕过。达到硬上限时停止创建新调用，保留已有结果和已产生用量并返回 `TASK_LIMIT_EXCEEDED`。MVP 不自动计算货币费用；保存供应商返回的用量，允许后续配置单价计算。

## 稳定错误码

至少包含：

| 错误码 | HTTP | 含义 |
|---|---:|---|
| `AUTH_REQUIRED` | 401 | 会话不存在或无效 |
| `SETUP_ALREADY_COMPLETED` | 409 | 唯一管理员已创建 |
| `VALIDATION_FAILED` | 400 | 请求不符合共享 Schema |
| `RESOURCE_NOT_FOUND` | 404 | 业务对象不存在 |
| `VERSION_CONFLICT` | 409 | 基于过期版本执行修改 |
| `CAPABILITY_DISABLED` | 422 | 请求依赖未配置能力 |
| `DOCUMENT_SPEC_NOT_CONFIRMED` | 409 | 规格尚未确认 |
| `BLOCK_LOCKED` | 409 | 目标块已锁定 |
| `RUN_NOT_CANCELABLE` | 409 | 当前状态不能取消 |
| `TASK_LIMIT_EXCEEDED` | 422 | 超过配置上限 |
| `PROVIDER_UNAVAILABLE` | 503 | 外部模型或 OpenViking 不可用 |
| `BACKUP_VALIDATION_FAILED` | 422 | 备份清单或哈希不一致 |

外部供应商错误映射为稳定错误码，同时把脱敏后的原始原因保存在运行历史中。
