# 退出同步人物草稿入口

## 完成范围

- 删除后台 `POST /api/v1/personas/draft` 路由，人物创建不再存在同步旁路。
- 删除未再挂载的人物草稿助手、专用共享输入、生成应用方法和提示变量构建代码。
- 删除只验证旧同步人物草稿流程的组件与集成测试。
- 接口事实源明确人物列表只创建可审计的人物蒸馏运行。

## 兼容边界

- 保留 `persona_draft` 算法编码、已发布提示词和既有数据库配置，避免改写历史配置与审计事实。
- 保留人物候选输出 Schema，人物蒸馏的灵魂综合步骤继续使用同一结构校验。
- 不增加旧入口到新人物蒸馏接口的转发层。

## 验证

- 旧同步人物草稿清理及门禁修复定向测试通过：4 个文件、57 项测试。
- `pnpm check` 通过：75 个测试文件、460 项测试、严格类型检查和生产构建全部成功。
- `pnpm test:e2e` 通过：4 项 Playwright 场景；核心闭环已改为人物蒸馏双检查点后创建人物。
