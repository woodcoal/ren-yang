# 精简顶部用户与主题入口开发记录

## 1. 调整范围

- 移除右上角管理员图标、名称和系统中心链接；侧栏底部的管理员与系统入口继续保留。
- 主题选择器收敛为无边框、无独立底色的单个调色板图标，不再长期占用顶部栏宽度显示主题名称。
- 雾白、暖砂、海盐、松岚分别使用灰、金、蓝、绿图标表示当前选择。
- 保留主题的中文选项、浏览器提示、无障碍名称和本机持久化能力。

## 2. 实现说明

- 删除顶部栏不再使用的管理员名称属性，布局不再向顶部栏传递该数据。
- 使用透明原生选择框覆盖调色板图标，保持键盘操作和系统原生选项列表。
- 主题变化后同步更新根节点主题、本机存储、图标颜色标识和中文提示。
- 更新组件与端到端测试，确认顶部栏不显示用户信息且主题图标状态正确。

## 3. 验证范围

- TypeScript：`pnpm exec tsc -p .nuxt/tsconfig.json --noEmit` 通过。
- Vitest：34 个测试文件、159 项测试全部通过；组件测试覆盖图标主题状态和右上角用户信息移除。
- Playwright：完整业务闭环通过，确认侧栏管理员入口保留、右上角不显示管理员名称、海盐主题显示蓝色状态图标。
- Nuxt 生产构建：`pnpm run build` 以及端到端测试内的独立构建均通过。
