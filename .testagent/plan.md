# 灵魂单文本模型测试计划

1. 数据模型与迁移
   - 验证旧 `runtime_summary` 迁移后等于新 `prompt_text`。
   - 验证草稿发布、历史复制和 Token 超限仍正确。
2. 手动与 AI 整理
   - 验证手动输入只做首尾规范化并形成未发布草稿。
   - 验证 AI 整理返回纯文本、保存草稿且不切换当前版本。
   - 验证模型未配置或输出无效时不保存草稿。
3. 前端交互
   - 验证一个大文本输入框、自动分析复选框和草稿确认发布流程。
   - 验证历史版本仍可复制为草稿。
4. 运行上下文
   - 验证快捷创建、兴趣判断、图文生成和分析只使用 `promptText`。
5. 门禁
   - 运行定向测试、全量测试、生产构建和迁移完整性检查。
