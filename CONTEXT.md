# AI 模拟对象与内容创作系统

本系统管理可持续迭代且可审计的模拟对象，并使用人物执行兴趣判断和结构化图文创作。本文只定义领域语言；实现约束见 `docs/`。

## 模拟对象

**模拟对象（Simulated Subject）**：
拥有灵魂和可校准成长提示词、可以形成运行上下文的业务对象。系统包含世界和人物两种模拟对象。
_避免使用_：智能体、机器人、提示词对象

**世界（World）**：
一种不具备记忆的特殊模拟对象，使用灵魂定义稳定世界规则，使用成长提示词承载从成长素材中提炼的有效变化。一个世界可以包含多个人物。
_避免使用_：全局资料库、人物背景、世界人物

**人物（Persona）**：
具备灵魂、成长提示词和记忆提示词并实际执行任务的模拟对象。创建时可按需使用参考资料，不再区分原创、资料型或混合型。一个具体人物只属于一个世界；跨世界复用时创建独立副本。
_避免使用_：智能体、角色机器人、小说人物

## 心智结构

**灵魂（Soul）**：
模拟对象稳定且必须完整保留的核心提示词，是受预算约束并直接进入新任务的一段纯文本。
_避免使用_：固定属性、人设字段、灵魂章节、运行摘要

**灵魂草稿（Soul Draft）**：
基于当前或指定历史灵魂版本创建、发布前允许编辑和删除的修改稿。
_避免使用_：候选人物版本、临时人格

**灵魂版本（Soul Version）**：
已发布且不可变的完整单文本灵魂快照，包含提示词和来源关系。
_避免使用_：人物版本、世界版本、配置版本

**当前灵魂版本（Active Soul Version）**：
模拟对象执行新任务时使用的已发布灵魂版本。
_避免使用_：最新版本

**成长素材（Growth Material）**：
用户从模拟对象资料库选取并评分的正文快照，或直接为成长添加并评分的文档；所有已启用素材共同用于提炼成长提示词。
_避免使用_：成长、人物反馈资料、普通证据

**成长提示词（Growth Prompt）**：
AI 综合全部已启用成长素材提炼、并经用户校准和发布的一份完整提示词；它不覆盖灵魂，并固定进入模拟对象的新任务。
_避免使用_：成长记录、自动训练结果、反馈资料

**处理记录（Operation Record）**：
人物完成写作、兴趣判断或分析等任务后形成的任务输入与结果快照，可由用户评分、启用或禁用，作为记忆素材。
_避免使用_：记忆、运行日志、会话原文

**记忆提示词（Memory Prompt）**：
AI 综合人物已启用处理记录提炼、并经用户校准和发布的一份完整提示词，描述人物从历史任务形成的兴趣、判断规律和可复用经验；世界没有记忆提示词。
_避免使用_：记忆记录、会话原文、全部历史、成长

**学习提示词草稿（Learning Prompt Draft）**：
AI 提炼或用户编辑形成、发布前不影响任务运行的成长提示词或记忆提示词修改稿。
_避免使用_：当前成长、当前记忆、自动生效结果

**学习提示词版本（Learning Prompt Version）**：
已发布且不可变的完整成长提示词或记忆提示词快照，可作为后续校准和回退的基线。
_避免使用_：成长记录、记忆记录、素材版本

**当前学习提示词（Active Learning Prompt）**：
模拟对象执行新任务时固定使用的已发布成长提示词或记忆提示词版本。
_避免使用_：最新分析结果、学习草稿

**分析批次（Analysis Batch）**：
一次可审计的世界成长、人物成长或人物记忆分析，固定素材快照、当前学习提示词、算法实现、算法配置、模型、参数、提示版本和 AI 提炼结果。
_避免使用_：后台任务、自动学习

**AI 接口连接（AI Connection）**：
一个可访问的 OpenAI-compatible 服务端点及其加密访问凭据。连接不代表具体模型，也不能直接被算法步骤选择。
_避免使用_：模型、供应商账号、明文密钥

**模型部署（Model Deployment）**：
某个 AI 接口连接上的具体文本或图片模型标识，是算法步骤选择模型的最小单位。
_避免使用_：接口、算法、参数方案

**AI 算法（AI Algorithm）**：
代码中固定步骤、顺序、输入处理和输出校验的 AI 处理流程。数据库不能改变流程，只能为固定步骤选择模型部署和调用参数。
_避免使用_：提示词、任意工作流、模型配置

**算法配置版本（Algorithm Configuration Version）**：
一次发布的完整算法步骤配置，固定每步模型部署、提示词编码和调用参数；新任务保存非敏感快照，后续编辑不追溯改变旧任务。
_避免使用_：算法实现、生成参数方案、当前环境变量

## 资料与证据

**参考资料（Source Material）**：
用户提供的文本内容，可与人物或世界关联并作为任务外部证据；普通参考资料不会自动成为成长或记忆。
_避免使用_：知识、记忆、成长资料

**世界资料（World Source）**：
与世界关联、可按任务相关性检索的参考资料；只有经用户选入成长素材后才参与世界成长提炼。
_避免使用_：世界灵魂、世界记忆

**证据（Evidence）**：
支持或反对某项判断、成长、记忆或内容生成结论的可定位原始片段。
_避免使用_：模型理由、参考答案

**证据快照（Evidence Snapshot）**：
一次运行或分析实际使用的证据内容、来源和哈希，保存后不随原资料或检索索引变化。
_避免使用_：检索日志

## 运行上下文与预算

**提示词预算（Prompt Budget）**：
模型调用前分配给总输入以及世界、人物、成长、记忆和参考资料的 Token 上限。
_避免使用_：字符上限、模型用量

**场景上下文（Scene Context）**：
只对一次运行生效的年龄阶段、地点、目标、情绪和当前事件，默认不进入灵魂、成长或记忆。
_避免使用_：人物记忆、临时人格

**上下文提供器（Context Provider）**：
在 SQLite 有效状态边界内检索任务相关参考资料的能力，可使用本地 FTS5 或 OpenViking；当前学习提示词不依赖检索。
_避免使用_：事实源、记忆数据库、向量库

## 运行与产物

**兴趣判断（Interest Assessment）**：
人物针对给定内容产生的结构化兴趣结论，包含概率、置信度、分项因素和证据。

**兴趣批次（Interest Batch）**：
同一人物针对一条或多条带稳定编号文本的一次批量模型调用。每条文本关联独立生成运行并保持输入顺序；单项格式或证据错误只终止对应运行，单项重试不携带旧模型回答，也不重跑其他条目。
_避免使用_：点赞预测、主观评分

**生成运行（Generation Run）**：
一次可追溯的兴趣判断或图文生成执行，固定世界和人物上下文、参数、模型、预算、提示、证据及适用的兴趣或文章算法配置快照。
_避免使用_：会话、聊天

**文档规格（Document Spec）**：
系统根据最终文章和配图分析自动形成的内部有序保存快照，定义正文与图片的顺序；不属于用户输入、确认或编辑对象。
_避免使用_：最终文档、提示词

**产物文档（Artifact Document）**：
一次生成运行形成的最终文章和可选图片结果；HTML 按正文位置混排图片，文本格式分别提供正文和图片数据。
_避免使用_：回复、消息

**产物块（Artifact Block）**：
系统内部保存和恢复正文段落或图片的执行单元，不向用户提供排序、锁定、尝试选择或单块重试操作。
_避免使用_：消息段、页面组件

**块尝试（Block Attempt）**：
某个产物块的一次实际生成，记录输入、输出、错误、用量和时间，历史尝试不可覆盖。
_避免使用_：块版本

## 反馈

**反馈事件（Feedback Event）**：
用户对兴趣判断、产物、参数、人物或资料作出的原始反馈记录，不等同于人物反馈资料。
_避免使用_：人物修改、训练数据

**灵魂修改提案（Soul Revision Proposal）**：
用户或分析过程提出的灵魂差异建议，确认后创建灵魂草稿，不直接覆盖当前灵魂版本。
_避免使用_：自动人格更新、成长提案
