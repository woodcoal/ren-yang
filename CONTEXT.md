# AI 模拟对象与内容创作系统

本系统管理可持续迭代且可审计的模拟对象，并使用人物执行兴趣判断和结构化图文创作。本文只定义领域语言；实现约束见 `docs/`。

## 模拟对象

**模拟对象（Simulated Subject）**：
拥有灵魂和成长、可以形成运行上下文的业务对象。系统包含世界和人物两种模拟对象。
_避免使用_：智能体、机器人、提示词对象

**世界（World）**：
一种不具备记忆的特殊模拟对象，使用灵魂定义稳定世界规则，使用成长承载从世界资料中提炼的有效变化。一个世界可以包含多个人物。
_避免使用_：全局资料库、人物背景、世界人物

**人物（Persona）**：
具备灵魂、成长和记忆并实际执行任务的模拟对象。创建时可按需使用参考资料，不再区分原创、资料型或混合型。一个具体人物只属于一个世界；跨世界复用时创建独立副本。
_避免使用_：智能体、角色机器人、小说人物

## 心智结构

**灵魂（Soul）**：
模拟对象稳定且必须完整保留的核心提示词，是受预算约束并直接进入新任务的一段纯文本。
_避免使用_：固定属性、人设字段、灵魂章节、运行摘要

**灵魂草稿（Soul Draft）**：
基于当前或指定历史灵魂版本创建、发布前允许编辑和删除的修改稿。
_避免使用_：候选人物版本、临时人格

**灵魂版本（Soul Version）**：
已发布且不可变的完整单文本灵魂快照，包含提示词和来源关系。
_避免使用_：人物版本、世界版本、配置版本

**当前灵魂版本（Active Soul Version）**：
模拟对象执行新任务时使用的已发布灵魂版本。
_避免使用_：最新版本

**成长原始资料（Growth Source）**：
用于提炼成长的原始数据。世界的成长原始资料是世界资料，人物的成长原始资料是人物反馈资料。
_避免使用_：成长、记忆、普通证据

**成长（Growth）**：
从已启用成长原始资料中分析并经用户确认的长期调整；它不覆盖灵魂，只在适用任务中作为附加规则参与运行。
_避免使用_：人物版本、自动训练结果、反馈资料

**处理记录（Operation Record）**：
人物执行写作、兴趣判断或分析等任务后形成的原始行为数据，可由用户启用或禁用，作为记忆分析证据。
_避免使用_：记忆、运行日志

**记忆（Memory）**：
从多条有效处理记录中提炼并经用户确认的兴趣、判断规律或可复用经验；世界没有记忆。
_避免使用_：会话原文、全部历史、成长

**分析批次（Analysis Batch）**：
一次可审计的世界成长、人物成长或人物记忆分析，固定输入证据、基线结论、模型、提示版本和分析结果。
_避免使用_：后台任务、自动学习

**迭代提案（Iteration Proposal）**：
分析批次提出的新增、修订、合并、取代或停用建议，必须经用户审核后才能改变有效成长或记忆。
_避免使用_：自动生效结果、修订提案

## 资料与证据

**参考资料（Source Material）**：
用户提供的文本内容，可与人物或世界关联并作为任务外部证据；普通参考资料不会自动成为成长或记忆。
_避免使用_：知识、记忆、成长资料

**人物反馈资料（Persona Feedback Source）**：
用户明确提供给人物学习的反馈原始条目，可启用、禁用或删除，只用于人物成长分析。
_避免使用_：普通反馈、人物记忆

**世界资料（World Source）**：
与世界关联且可作为世界成长原始资料的内容，在 OpenViking 中投影为对应 User Resource。
_避免使用_：世界灵魂、世界记忆

**证据（Evidence）**：
支持或反对某项判断、成长、记忆或内容生成结论的可定位原始片段。
_避免使用_：模型理由、参考答案

**证据快照（Evidence Snapshot）**：
一次运行或分析实际使用的证据内容、来源和哈希，保存后不随原资料或检索索引变化。
_避免使用_：检索日志

## 运行上下文与预算

**提示词预算（Prompt Budget）**：
模型调用前分配给总输入以及世界、人物、成长、记忆和参考资料的 Token 上限。
_避免使用_：字符上限、模型用量

**场景上下文（Scene Context）**：
只对一次运行生效的年龄阶段、地点、目标、情绪和当前事件，默认不进入灵魂、成长或记忆。
_避免使用_：人物记忆、临时人格

**上下文提供器（Context Provider）**：
在 SQLite 有效状态边界内检索相关成长、记忆和参考资料的能力，可使用本地 FTS5 或 OpenViking。
_避免使用_：事实源、记忆数据库、向量库

## 运行与产物

**兴趣判断（Interest Assessment）**：
人物针对给定内容产生的结构化兴趣结论，包含概率、置信度、分项因素和证据。
_避免使用_：点赞预测、主观评分

**生成运行（Generation Run）**：
一次可追溯的兴趣判断或图文生成执行，固定世界和人物上下文、参数、模型、预算、提示及证据快照。
_避免使用_：会话、聊天

**文档规格（Document Spec）**：
正式生成前经用户确认的有序图文计划，定义每个块的用途、要求、依赖和验收规则。
_避免使用_：最终文档、提示词

**产物文档（Artifact Document）**：
一次生成运行形成的有序图文结果，可渲染为 HTML、Markdown 或 Txt。
_避免使用_：回复、消息

**产物块（Artifact Block）**：
产物文档中的一个文字块或图片块，具有独立规格、状态和当前选中结果。
_避免使用_：消息段、页面组件

**块尝试（Block Attempt）**：
某个产物块的一次实际生成，记录输入、输出、错误、用量和时间，历史尝试不可覆盖。
_避免使用_：块版本

## 反馈与评测

**反馈事件（Feedback Event）**：
用户对兴趣判断、产物、参数、人物或资料作出的原始反馈记录，不等同于人物反馈资料。
_避免使用_：人物修改、训练数据

**灵魂修改提案（Soul Revision Proposal）**：
用户或分析过程提出的灵魂差异建议，确认后创建灵魂草稿，不直接覆盖当前灵魂版本。
_避免使用_：自动人格更新、成长提案

**评测用例（Evaluation Case）**：
用于比较模拟对象变化前后行为的固定输入、预期约束和评分规则。
_避免使用_：测试提示词

**评测运行（Evaluation Run）**：
灵魂草稿或迭代提案对固定评测用例的一次执行及结果集合。
_避免使用_：单元测试、模型自评
