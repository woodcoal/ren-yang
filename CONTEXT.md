# AI 人物模拟与反馈迭代系统

本系统管理可版本化的模拟人物，以人物资料和已确认反馈为依据，执行兴趣判断与结构化图文生成。本文只定义领域语言；实现约束见 `docs/`。

## 人物与世界

**模拟人物（Persona）**：
被系统模拟的逻辑人物，可以完全原创、来自参考资料或由两者混合建立。
_避免使用_：智能体、角色机器人、小说人物

**人物来源模式（Persona Origin）**：
人物的建立方式，取值为原创、资料型或混合型，不决定人物是否可以继续附加资料。
_避免使用_：人物类型、模型类型

**人物版本（Persona Version）**：
某一时点完整且不可变的人物快照，包含人物事实、稳定偏好、表达规则和约束。
_避免使用_：人物配置、当前提示词

**当前人物版本（Active Persona Version）**：
人物执行新任务时默认使用的已发布人物版本。
_避免使用_：最新版本

**候选人物版本（Candidate Persona Version）**：
尚未发布的人物快照，必须经过适用的评测和发布规则后才能成为当前人物版本。
_避免使用_：草稿提示词、临时人格

**世界设定（World）**：
可选的共享背景，承载多个人物共同使用的世界观、地点、时间线和事件。
_避免使用_：全局资料库、人物背景

**场景上下文（Scene Context）**：
只对一次运行生效的年龄阶段、地点、目标、情绪和当前事件，默认不进入长期人物版本。
_避免使用_：人物记忆、临时人格

## 资料与证据

**参考资料（Source Material）**：
用户提供的文本内容，MVP 通过粘贴、TXT 或 Markdown 导入，可与人物或世界设定关联。
_避免使用_：知识、记忆

**资料角色（Source Role）**：
参考资料的用途分类，MVP 包括原著事实、普通参考和表达样例。世界设定由独立的世界对象承载，不是资料角色。
_避免使用_：资料标签

**证据（Evidence）**：
支持或反对某项人物判断、内容生成或人物修订的可定位资料片段。
_避免使用_：模型理由、参考答案

**证据快照（Evidence Snapshot）**：
一次运行实际使用的证据内容、来源和哈希，保存后不随原资料或检索索引变化。
_避免使用_：检索日志

**上下文提供器（Context Provider）**：
根据人物和任务检索证据的能力，MVP 可选择本地全文检索或 OpenViking 语义检索。
_避免使用_：记忆数据库、向量库

## 运行与产物

**兴趣判断（Interest Assessment）**：
人物针对给定内容产生的结构化兴趣结论，包含概率、置信度、分项因素和证据。
_避免使用_：点赞预测、主观评分

**生成运行（Generation Run）**：
一次可追溯的兴趣判断或图文生成执行，固定人物版本、参数、模型、提示和证据快照。
_避免使用_：会话、聊天

**文档规格（Document Spec）**：
正式生成前经用户确认的有序图文计划，定义每个块的用途、要求、依赖和验收规则。
_避免使用_：最终文档、提示词

**产物文档（Artifact Document）**：
一次生成运行形成的有序图文结果，可渲染为 HTML、Markdown 或 Txt。
_避免使用_：回复、消息

**产物块（Artifact Block）**：
产物文档中的一个文字块或图片块，具有独立规格、状态和当前选中结果。
_避免使用_：消息段、页面组件

**块尝试（Block Attempt）**：
某个产物块的一次实际生成，记录输入、输出、错误、用量和时间，历史尝试不可覆盖。
_避免使用_：块版本

## 反馈与评测

**反馈事件（Feedback Event）**：
用户对兴趣判断、产物、参数、人物或资料作出的原始反馈记录。
_避免使用_：人物修改、训练数据

**修订提案（Revision Proposal）**：
由用户明确确认的长期人物反馈产生的可审查人物差异；一次反馈最多形成一个提案，保存目标字段、修改理由和风险等级。
_避免使用_：自动学习结果

**候选记忆（Candidate Memory）**：
从用户明确确认的长期人物反馈中提取、并已关联修订提案的长期人物假设。场景和普通反馈不会自动形成候选记忆。
_避免使用_：长期记忆

**评测用例（Evaluation Case）**：
用于比较人物版本行为的固定输入、预期约束和评分规则。
_避免使用_：测试提示词

**评测运行（Evaluation Run）**：
人物候选版本对固定评测用例的一次执行及结果集合。
_避免使用_：单元测试、模型自评
